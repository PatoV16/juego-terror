Environment example
NODE_ENV=development PORT=3000 VITE_API_URL=http://localhost:3000/api

Commit message: Add .env.example

.github/workflows/ci.yml Contenido: name: CI
on: [push, pull_request]

jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - uses: actions/setup-node@v4 with: node-version: 20 cache: 'npm' - run: npm ci - run: npm run lint || true - run: npm run build - uses: actions/upload-artifact@v4 with: name: web-dist path: dist
